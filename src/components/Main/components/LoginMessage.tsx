import React from "react";
import RotatingMessage from "./RotatingMessage";

const LoginMessage = () => {
  return (
    <section>
      <RotatingMessage />
    </section>
  );
};

export default LoginMessage;
